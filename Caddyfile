{
    frankenphp
    auto_https off
    admin off
}

:{$PORT:80} {
    root * /app/web
    encode zstd gzip
    php_server
    
    # WordPress Permalinks
    try_files {path} {path}/ /index.php?{query}

    log {
        output stdout
        format json
    }
}
